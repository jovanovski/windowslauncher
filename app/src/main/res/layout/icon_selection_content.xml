<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- Which icon set to show -->
    <LinearLayout
        android:id="@+id/icon_type_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:layout_marginBottom="12dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:text="Show icons from:"
            android:textColor="#000000"
            android:textSize="12sp"
            android:fontFamily="@font/micross_block" />

        <Spinner
            android:id="@+id/icon_set_spinner"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/spinner_background"
            android:padding="2dp" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/icons_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="330dp"
        android:paddingEnd="30dp"
        android:clipToPadding="false"
        android:scrollbars="vertical"
        android:scrollbarStyle="outsideOverlay"
        android:scrollbarAlwaysDrawVerticalTrack="true"
        android:scrollbarTrackVertical="@drawable/scrollbar_track_98"
        android:scrollbarThumbVertical="@drawable/win98_start_menu_border"
        android:scrollbarSize="16dp"
        android:scrollbarFadeDuration="0"
        android:scrollbarDefaultDelayBeforeFade="0"
        android:fadeScrollbars="false"
        />

    <!-- Pick an icon from an image on the device -->
    <LinearLayout
        android:id="@+id/browse_icon_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="12dp">

        <TextView
            android:id="@+id/btn_browse_icon"
            style="@android:style/Widget.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:text="Browse..."
            android:textSize="12sp"
            android:fontFamily="@font/micross_block"
            android:background="@drawable/win98_start_menu_border"
            android:backgroundTint="@null" />

    </LinearLayout>

</LinearLayout>