Custom Icons Folder
==================

Place WebP images in this folder to use as custom desktop icons.

Requirements:
- Only WebP format is supported
- Recommended size: 256x256 pixels
- File names should not contain special characters
- Icons will be displayed in a 4-column grid
- Supports transparency (alpha channel)

The first icon shown will always be the app's default icon.